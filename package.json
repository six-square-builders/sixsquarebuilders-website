{
  "homepage": "https://sixsquarebuilders.co.in", 
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "react-router-dom": "^7.0.1",
    "react-scripts": "^5.0.1" 
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
