version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sixsquarebuilders
    ports:
      - "3000:80"
    environment:
      NODE_ENV: production
    restart: unless-stopped
